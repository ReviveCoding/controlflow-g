# Denominator contracts

The predeclared contracts and null-fail policies are in `configs/v24/denominator_contract.yaml`; gate definitions are in `configs/v24/qualification_gates.yaml`. Independent methodology review approved minimum denominators of 120 critical, 150 stale-policy, 100 authoritative REQUIRE_REVIEW, and 50 authoritative DENY attempts (`state/v24_prequalification_review_findings.json`). The post-execution independent recomputation reported valid denominators in `results/v24/qualification/denominators.json`. That table is diagnostic because post-close verification failed.
