# V2 Ablation Status

A release-grade controlled ablation was not run. The earlier `results/v2/ablation.parquet` contains development scenario diagnostics and counterfactual counts, not causal one-factor ablations. Round 4 remained below the STC gate, so spending full runtime on ablations would violate the phase rule.
