# Limitations

Development data was circular, the provisional bundle did not control execution, calibration-only threshold provenance was absent, policy/approval authority was not cryptographically bound, temporal evaluation was vacuous, and vLLM xgrammar smoke could not complete because an existing GPU workload left insufficient cache memory. Local Python latency is not model-serving latency.
