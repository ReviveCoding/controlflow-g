# Future work

Build independent TRAIN/CALIBRATION/VALIDATION generators and fitted artifacts; select threshold on CALIBRATION only; require the exact bundle at runtime; implement signed externally issued approvals and PDP recomputation inside PEP; add transactional crash recovery; construct independent temporal truth and distractor retrieval; integrate checkpoint/integrity eligibility; rerun all three reviews before any qualification generation.
