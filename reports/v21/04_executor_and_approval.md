# Executor and approval

SQLite uniqueness tests cover duplicate commits and restart replay, but tokens are unsigned and candidate execution self-issues approvals. Transactional simulated state, crash injection, authoritative PDP recomputation, and external reviewer issuance remain required. See `state/v21_review_findings.json`.
