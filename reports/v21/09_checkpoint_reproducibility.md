# Checkpoint reproducibility

Fingerprint field and mutation unit tests exist, but checkpoint creation and resume validation are not connected to candidate execution. `checkpoint_binding_violations` is therefore nonzero in `state/v21_integrity.json`.
