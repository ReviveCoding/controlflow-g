# V2.2 PDP and PEP

The PEP accepts a proposed action and authenticated context, canonicalizes the action, and obtains its own authoritative PDP decision. The explicit registry denies unknown actions. The PDP is queried again inside the commit transaction using current authorization state.
