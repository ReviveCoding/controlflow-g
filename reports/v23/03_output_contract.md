# V2.3 Output Contract

In the paired interactivity/160-token ablation, the current schema measured 23.700s total P95 and the semantically verified minimal enum contract measured 8.933s with structured failure 0.0000. Deterministic safety fields remain assembled by the typed pipeline; the LLM returns only the summary and constrained evidence count.
