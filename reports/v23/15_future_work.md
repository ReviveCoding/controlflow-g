# V2.3 Future Work

For a future version—not a V23QUAL rerun—predeclare a fresh generator that guarantees a nonzero, representative stale-policy stratum while preserving deterministic truth and holdout independence. Close and checkpoint SQLite connections before computing final artifact bindings, then verify those bindings from a separate process. Externalize signing keys and audit anchors, repeat hardware characterization on deployment-class accelerators, and add independent human evaluation of explanation usefulness.
