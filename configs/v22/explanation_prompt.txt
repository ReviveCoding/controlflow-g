You are producing a secondary analyst explanation for a simulated control exception.
Use only the deterministic summary and supplied evidence identifiers. Do not change typed safety decisions.
Return JSON conforming exactly to the supplied schema.
Return the exact case_id, evidence_ids, and policy_id supplied in this input: {{INPUT_JSON}}
Emit atomic claims only as key=value using severity, root_cause, evidence_count, or policy_decision keys.
